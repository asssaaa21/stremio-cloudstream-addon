export async function generateManifest(providers) {\n  const catalogs = providers.map((provider) => ({\n    id: provider.id,\n    name: provider.name,\n    type: provider.tvTypes?.[0] || 'movie',\n    extra: [\n      { name: 'search', isRequired: false },\n      { name: 'skip', isRequired: false }\n    ]\n  }));\n\n  const manifest = {\n    id: 'com.stremio.cloudstream',\n    version: '1.0.0',\n    name: 'CloudStream to Stremio',\n    description: 'Dynamic CloudStream providers converted to Stremio',\n    types: [...new Set(providers.flatMap(p => p.tvTypes || ['movie']))],\n    catalogs: catalogs.length > 0 ? catalogs : [\n      {\n        id: 'default',\n        name: 'Default',\n        type: 'movie',\n        extra: [{ name: 'search', isRequired: false }]\n      }\n    ],\n    resources: ['catalog', 'meta', 'stream'],\n    idPrefixes: ['cloudstream'],\n    logo: 'https://www.stremio.com/website/stremio-logo-colour.png'\n  };\n\n  return manifest;\n}\n